#wlniao/dotnet

### Version
```
This docker imager build for the .NET Core 3.1.0
```

### Build
```
git clone https://github.com/wln-docker/dotnet.git && docker build -t wlniao/dotnet:3.1.0 --no-cache ./dotnet/3.1.0
```