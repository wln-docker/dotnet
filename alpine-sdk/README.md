#wlniao/dotnet

### Version
```
This docker imager build for the .NET Core 6.0.0
```

### Build
```
git clone https://github.com/wln-docker/dotnet.git && docker build -t wlniao/dotnet:alpine-sdk --no-cache ./dotnet/alpine-sdk
```